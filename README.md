# Tracker System

Этот репозиторий содержит две независимые Vite+React приложения:

- `tracker-site` — публичный трекер
- `admin-site` — закрытая админ-панель

Каждое приложение содержит `.env.example`. Создайте `.env.local` в каждой папке и заполните значения Firebase.
